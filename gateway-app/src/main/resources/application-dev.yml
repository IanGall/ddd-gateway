dubbo:
  consumer:
    parameters:
      ssl-enabled: false
  registry:
    address: ${DUBBO_REGISTRY_ADDRESS:nacos://127.0.0.1:8848}
    username: ${DUBBO_REGISTRY_USERNAME:nacos}
    password: ${DUBBO_REGISTRY_PASSWORD:Ian2026OpsPwd}
    use-as-config-center: false
    use-as-metadata-center: false

oauth2:
  client:
    id: ${OAUTH2_CLIENT_ID:integration-client}
    secret: ${OAUTH2_CLIENT_SECRET:change-me-in-production}
  internal-token: ${OAUTH2_INTERNAL_TOKEN:${OAUTH2_CLIENT_SECRET:change-me-in-production}}
