# gateway-app 模块协作说明

## 模块定位

- 本模块是可运行的网关参考应用，负责 HTTP 接入、参数校验、协议适配和下游服务调用编排。
- 业务规则必须由下游 DDD 服务承载，网关只依赖其公开 API 契约。

## 依赖约束

- 统一继承网关聚合根，由聚合根导入 `ddd-base-bom` 并管理实际使用的标准 API 版本。
- RPC 调用统一使用 Dubbo Triple，避免重试放大写请求和超时请求。
- 公共响应与异常模型复用 `ddd-common`，禁止新增重复公共模块或 Hutool 依赖。

## 提交前检查

- 校验入口参数、认证规则与异常响应语义。
- 评估远程调用次数，避免 N+1 或重复 RPC。
- 执行当前模块测试，并通过根聚合工程的完整验证。
